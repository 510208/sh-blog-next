---
const { class: classFromProps, className, ...props } = Astro.props;

// 如果沒有傳入 className，則使用預設的 class
const finalClass = classFromProps || className || "group";
---

<blockquote class:list={finalClass} {...props}>
  <slot />
</blockquote>
