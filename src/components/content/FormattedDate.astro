---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString("zh-Hant", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
